# price-fetcher

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build price-fetcher` to build the library.

## Running unit tests

Run `nx test price-fetcher` to execute the unit tests via [Jest](https://jestjs.io).
