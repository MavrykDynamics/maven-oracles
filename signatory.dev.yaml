server:
  # Address/Port that Signatory listens on
  address: :6732
  # Address/Port that Signatory serves prometheus metrics on
  utility_address: :9583

vaults:
  # Name of vault
  local_file_keys:
    driver: file
    config:
      file: /etc/secret.json

# List enabled public keys hashes here
mavryk:
  mv1AAFByPGTyFdbshgfA8ogjgf7pQKuR7ATp: # Oracle 1
    log_payloads: true
    allowed_operations:
      # List of [generic, block, endorsement]
      - generic
      - failing_noop
    allowed_kinds:
      # List of [endorsement, ballot, reveal, transaction, origination, delegation, seed_nonce_revelation, activate_account]
      - transaction

  # Default policy allows "block" and "endorsement" operations
  mv1B1CtqbiFwtWFkqrFbDAMRbDTofCSjHokn: # Oracle 2
    log_payloads: true
    allowed_operations:
      # List of [generic, block, endorsement]
      - generic
      - failing_noop
    allowed_kinds:
      # List of [endorsement, ballot, reveal, transaction, origination, delegation, seed_nonce_revelation, activate_account]
      - transaction
  mv1BpFE1wRGoAxrp52p5bMxEF5uztiYNshBJ: # Oracle 3
    log_payloads: true
    allowed_operations:
      # List of [generic, block, endorsement]
      - generic
      - failing_noop
    allowed_kinds:
      # List of [endorsement, ballot, reveal, transaction, origination, delegation, seed_nonce_revelation, activate_account]
      - transaction
  mv18DheWgrqRXba8t56o6K2Y51NGo4gMucGx: # Oracle 4
    log_payloads: true
    allowed_operations:
      # List of [generic, block, endorsement]
      - generic
      - failing_noop
    allowed_kinds:
      # List of [endorsement, ballot, reveal, transaction, origination, delegation, seed_nonce_revelation, activate_account]
      - transaction
  mv1LhKD4FHjabSrz4bYWEzEfgDxZezuJbGc2: # Oracle 5
    log_payloads: true
    allowed_operations:
      # List of [generic, block, endorsement]
      - generic
      - failing_noop
    allowed_kinds:
      # List of [endorsement, ballot, reveal, transaction, origination, delegation, seed_nonce_revelation, activate_account]
      - transaction
  mv1FmXyQkcfufUw9zwW3Dyg1W3JMHcDZ8rV5: # Oracle 6
    log_payloads: true
    allowed_operations:
      # List of [generic, block, endorsement]
      - generic
      - failing_noop
    allowed_kinds:
      # List of [endorsement, ballot, reveal, transaction, origination, delegation, seed_nonce_revelation, activate_account]
      - transaction
  mv1PkouN3EwYmnfjFk817r7mXzTmjY1r5M1e: # Oracle 7
    log_payloads: true
    allowed_operations:
      # List of [generic, block, endorsement]
      - generic
      - failing_noop
    allowed_kinds:
      # List of [endorsement, ballot, reveal, transaction, origination, delegation, seed_nonce_revelation, activate_account]
      - transaction
