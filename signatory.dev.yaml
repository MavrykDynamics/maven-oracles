server:
  # Address/Port that Signatory listens on
  address: :6732
  # Address/Port that Signatory serves prometheus metrics on
  utility_address: :9583

vaults:
  # Name of vault
  local_file_keys:
    driver: file
    config:
      file: /etc/secret.json

# List enabled public keys hashes here
tezos:
  tz1MnmtP4uAcgMpeZN6JtyziXeFqqwQG6yn6: # Oracle 1
    log_payloads: true
    allowed_operations:
      # List of [generic, block, endorsement]
      - generic
      - failing_noop
    allowed_kinds:
      # List of [endorsement, ballot, reveal, transaction, origination, delegation, seed_nonce_revelation, activate_account]
      - transaction

  # Default policy allows "block" and "endorsement" operations
  tz1NdjbFHLxbKEUXhXgjyLfQSCbq6oy5b7Px: # Oracle 2
    log_payloads: true
    allowed_operations:
      # List of [generic, block, endorsement]
      - generic
      - failing_noop
    allowed_kinds:
      # List of [endorsement, ballot, reveal, transaction, origination, delegation, seed_nonce_revelation, activate_account]
      - transaction
  tz1PSmvRd3ySbh5aviFEMYGD6542LL5QnrMk: # Oracle 3
    log_payloads: true
    allowed_operations:
      # List of [generic, block, endorsement]
      - generic
      - failing_noop
    allowed_kinds:
      # List of [endorsement, ballot, reveal, transaction, origination, delegation, seed_nonce_revelation, activate_account]
      - transaction
  tz1KrELvNVY4xKnujkXwrVLWuzWJEg9FvA8v: # Oracle 4
    log_payloads: true
    allowed_operations:
      # List of [generic, block, endorsement]
      - generic
      - failing_noop
    allowed_kinds:
      # List of [endorsement, ballot, reveal, transaction, origination, delegation, seed_nonce_revelation, activate_account]
      - transaction
  tz1YKquTvvSE2B5kvGyf1AYeXD6b6cMDSzDe: # Oracle 5
    log_payloads: true
    allowed_operations:
      # List of [generic, block, endorsement]
      - generic
      - failing_noop
    allowed_kinds:
      # List of [endorsement, ballot, reveal, transaction, origination, delegation, seed_nonce_revelation, activate_account]
      - transaction
  tz1TQ4fpSFNZ6D9vrcwBz9yzM2SNjDj5YoU7: # Oracle 6
    log_payloads: true
    allowed_operations:
      # List of [generic, block, endorsement]
      - generic
      - failing_noop
    allowed_kinds:
      # List of [endorsement, ballot, reveal, transaction, origination, delegation, seed_nonce_revelation, activate_account]
      - transaction
  tz1bPLbmiseCCWtW7RZ9t2RkNyboB9XT4exJ: # Oracle 7
    log_payloads: true
    allowed_operations:
      # List of [generic, block, endorsement]
      - generic
      - failing_noop
    allowed_kinds:
      # List of [endorsement, ballot, reveal, transaction, origination, delegation, seed_nonce_revelation, activate_account]
      - transaction
